{# json_rules 用途：结构化输出的统一 JSON 契约（05 §2.3 决定 #4）· 版本 1 · 2026-09-05 #}
输出契约（必须严格遵守）：
1. 只输出一个 JSON 对象，放在 ```json 代码围栏内，围栏外不得有任何文字。
2. JSON 字段名必须与任务指定完全一致；不得新增、重命名或翻译字段名。
3. 不输出注释、省略号、示例或解释性文字。
4. 数组字段无内容时输出空数组 []；文本字段无内容时输出空字符串 ""。